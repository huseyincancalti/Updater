<UserControl x:Class="ZenUpdate.App.Views.WindowsUpdatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Windows Updates" Style="{StaticResource PageTitleStyle}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Check for Updates"
                    Command="{Binding ScanCommand}"
                    IsEnabled="False"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0" />
            <ProgressBar IsIndeterminate="True"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                         Width="100"
                         VerticalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="2"
                Padding="24"
                CornerRadius="8"
                BorderThickness="1"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                Background="{DynamicResource MaterialDesignPaper}">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="This module is not implemented yet."
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8" />
                <TextBlock Text="Windows Update support will appear here in a future pass."
                           Opacity="0.7"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Opacity="0.6" Margin="0,8,0,0" FontSize="12" />
    </Grid>
</UserControl>
