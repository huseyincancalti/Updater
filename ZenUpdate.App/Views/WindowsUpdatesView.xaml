<UserControl x:Class="ZenUpdate.App.Views.WindowsUpdatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <!--
        DataContext is set automatically by MainWindow's DataTemplate binding.
        DataContext type: WindowsUpdatesViewModel
    -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Windows Updates"
                   Style="{StaticResource PageTitleStyle}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">

            <Button Content="Check for Updates"
                    Command="{Binding ScanCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0" />

            <Button Content="Cancel"
                    Command="{Binding CancelScanCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <ProgressBar IsIndeterminate="True"
                         Width="120"
                         Height="4"
                         VerticalAlignment="Center"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />

        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Updates}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  materialDesign:DataGridAssist.CellPadding="8 6"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8 6">
            <DataGrid.Columns>

                <!--
                    Star-width column with tooltip for long titles.
                    SortMemberPath keeps column-header click sorting working.
                -->
                <DataGridTemplateColumn Header="Update"
                                        Width="*"
                                        IsReadOnly="True"
                                        SortMemberPath="DisplayName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"
                                       Padding="2,0"
                                       ToolTip="{Binding DisplayName}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="KB Article"
                                    Binding="{Binding KbArticleId}"
                                    Width="100"
                                    IsReadOnly="True" />

                <DataGridTextColumn Header="Severity"
                                    Binding="{Binding MsrcSeverity}"
                                    Width="100"
                                    IsReadOnly="True" />

                <DataGridTextColumn Header="Status"
                                    Binding="{Binding Status}"
                                    Width="90"
                                    IsReadOnly="True" />

            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Opacity="0.65"
                   FontSize="12"
                   Margin="0,8,0,0" />

    </Grid>
</UserControl>
