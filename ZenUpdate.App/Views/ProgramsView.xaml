<UserControl x:Class="ZenUpdate.App.Views.ProgramsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <!--
        DataContext is set automatically by MainWindow's DataTemplate binding.
        DataContext type: ProgramsViewModel
    -->
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- Page title -->
            <RowDefinition Height="Auto" />   <!-- Toolbar   -->
            <RowDefinition Height="*"    />   <!-- DataGrid  -->
            <RowDefinition Height="Auto" />   <!-- Status bar -->
        </Grid.RowDefinitions>

        <!-- ===== Page Title ===== -->
        <TextBlock Grid.Row="0"
                   Text="Programs"
                   Style="{StaticResource PageTitleStyle}" />

        <!-- ===== Toolbar ===== -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">

            <Button Content="Scan for Updates"
                    Command="{Binding ScanCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0" />

            <Button Content="Cancel"
                    Command="{Binding CancelScanCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0"
                    Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <Button Content="Update Selected"
                    Command="{Binding InstallSelectedCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Margin="0,0,8,0" />

            <!-- Spinner shown while scanning -->
            <ProgressBar IsIndeterminate="True"
                         Width="120"
                         Height="4"
                         VerticalAlignment="Center"
                         Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />

        </StackPanel>

        <!-- ===== Updates DataGrid ===== -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Updates}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="False"
                  SelectionMode="Single"
                  materialDesign:DataGridAssist.CellPadding="8 6"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8 6">
            <DataGrid.Columns>

                <!-- Selection checkbox -->
                <DataGridCheckBoxColumn Header=""
                                        Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}"
                                        Width="40" />

                <!-- Application display name -->
                <DataGridTextColumn Header="Application"
                                    Binding="{Binding DisplayName}"
                                    Width="*"
                                    IsReadOnly="True" />

                <!-- Publisher from AppUpdateItem -->
                <DataGridTextColumn Header="Publisher"
                                    Binding="{Binding Publisher}"
                                    Width="160"
                                    IsReadOnly="True" />

                <!-- Installed version -->
                <DataGridTextColumn Header="Current Version"
                                    Binding="{Binding CurrentVersion}"
                                    Width="130"
                                    IsReadOnly="True" />

                <!-- Available (new) version -->
                <DataGridTextColumn Header="New Version"
                                    Binding="{Binding AvailableVersion}"
                                    Width="130"
                                    IsReadOnly="True" />

                <!-- Lifecycle status -->
                <DataGridTextColumn Header="Status"
                                    Binding="{Binding Status}"
                                    Width="100"
                                    IsReadOnly="True" />

            </DataGrid.Columns>
        </DataGrid>

        <!-- ===== Status Bar ===== -->
        <TextBlock Grid.Row="3"
                   Text="{Binding StatusMessage}"
                   Opacity="0.65"
                   FontSize="12"
                   Margin="0,8,0,0" />

    </Grid>
</UserControl>
