<UserControl x:Class="ZenUpdate.App.Views.DriversView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Drivers" Style="{StaticResource PageTitleStyle}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Scan for Driver Updates"
                    Command="{Binding ScanCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,0,8,0" />
            <ProgressBar IsIndeterminate="True"
                                        Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        Width="100"
                                        VerticalAlignment="Center" />
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Updates}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  materialDesign:DataGridAssist.CellPadding="8 4"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8 4">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="40" IsReadOnly="False" />
                <DataGridTextColumn Header="Device" Binding="{Binding DisplayName}" Width="*" />
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="180" />
                <DataGridTextColumn Header="Class" Binding="{Binding DeviceClass}" Width="120" />
                <DataGridTextColumn Header="New Version" Binding="{Binding AvailableVersion}" Width="160" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Opacity="0.6" Margin="0,8,0,0" FontSize="12" />
    </Grid>
</UserControl>
